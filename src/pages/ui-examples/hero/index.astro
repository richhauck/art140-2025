---
const title = "Hero";
import Layout from "../../../layouts/Layout.astro";
---

<Layout title={title}>
  <iframe
    src={`${import.meta.env.BASE_URL}ui-examples/hero/example/index.html`}
    style="width:100%; height: 600px;"></iframe>
  <p>
    A hero section in an HTML page refers to the prominent, often full-width,
    area at the top of a website or landing page. It is typically the first
    visual element a visitor encounters and serves to immediately capture
    attention and convey the page's core message or purpose.
  </p>
  <a
    class="button"
    href={`${import.meta.env.BASE_URL}ui-examples/hero/example/index.html`}
    >View Example</a
  >
</Layout>
