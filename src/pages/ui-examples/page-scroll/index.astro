---
const title = "Page Scroll";
import Layout from "../../../layouts/Layout.astro";
import { Code } from "astro:components";
---

<Layout title={title}>
  <iframe
    src={`${import.meta.env.BASE_URL}ui-examples/page-scroll/example/index.html`}
    style="width:100%; height: 300px;"></iframe>
  <a
    class="button"
    href={`${import.meta.env.BASE_URL}ui-examples/page-scroll/example/index.html`}
    >View Example</a
  >
  <p>
    Implementing page scrolling is as easy as linking to an id within the page.
  </p>
  <Code
    code={`<a href="#top">Back to top</a>
`}
    lang="html"
  />
  <p>
    This will create a link that jumps directly to that id. To enable smooth
    scrolling behavior, you can add the following CSS to your stylesheet:
  </p>
  <Code
    code={`html {
  scroll-behavior: smooth;
}
`}
    lang="css"
  />
  <p>
    This CSS rule ensures that when users click on anchor links, the page will
    scroll smoothly to the target section instead of jumping abruptly.
  </p>
</Layout>
