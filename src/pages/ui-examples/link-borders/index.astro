---
const title = "Link Borders";
import Layout from "../../../layouts/Layout.astro";
import { Code } from "astro:components";
---

<Layout title={title}>
  <p>
    Instead of adding characters between hyperlinks, use CSS borders instead, as
    this is more semantically correct and ensures better legibility for search
    engines.
  </p>
  <iframe
    src={`${import.meta.env.BASE_URL}ui-examples/link-borders/example.html`}
    style="width:100%; height: 55px; margin-bottom: 10px;"></iframe>

  <a
    class="button"
    href={`${import.meta.env.BASE_URL}ui-examples/link-borders/example.html`}
    >View Example</a
  >
  <Code
    code={`/* Removes default padding that offsets a list */
#primary-nav ul {
  padding-left: 0;
}
  
/* Removes default padding */
#primary-nav li {
  list-style: none;
  display: inline-block;
  border-left: 1px solid gray;
  padding: 0 10px;
}
/* Overrides above rule for first list item */
#primary-nav li:first-child {
  border-left: none;
}

`}
    lang="css"
  />
  <p>Notice that the last child omits the border.</p>
</Layout>
